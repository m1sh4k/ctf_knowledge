	o#### SYN/ACK and TCP Handshake
In Wireshark, filtering with `tcp.flags == 0x012` allows you to observe SYN/ACK packets 
#### http simple authentication
`http.authbasic`

#### connection conversations
`-> Statiscs -> Conversations`